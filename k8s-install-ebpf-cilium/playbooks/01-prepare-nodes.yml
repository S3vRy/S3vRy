---
# Playbook for node preparation

- name: Node preparation for Kubernetes
  hosts: k8s_masters
  gather_facts: true
  become: true
  vars_files:
    - ../group_vars/all.yml

  roles:
    - prepare-nodes

