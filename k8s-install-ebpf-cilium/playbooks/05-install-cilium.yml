---
# Playbook for Cilium installation

- name: Cilium installation
  hosts: k8s-master-1
  gather_facts: true
  become: true
  vars_files:
    - ../group_vars/all.yml

  roles:
    - install-cilium

